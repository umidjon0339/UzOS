import string

font = bytearray(256 * 16)
# simple font for demo: just blocks for non-printable, or standard
# Since creating a full 8x16 font from scratch in python is complex without a library,
# let's just make a simple 8x8 font, or a placeholder if I don't want to use a real font file.
